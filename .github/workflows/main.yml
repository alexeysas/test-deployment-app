on:
  push:
    branches:
      - main

jobs:
  example:
    runs-on: ubuntu-latest
    steps:  
    - id: foo
      if: false
      uses: alexeysas/test-deployment-app@v1
      with:
        who-to-greet: 'Mona the Octocat'
    - name: First1222
      if: true
      run: |
        echo first
    - name: First12223
      if: true
      run: |
        echo dsds
    - name: First12333
      id: first22
      if: ${{ !always() }}
      run: |
        echo before bad
    - name: bad
      run: | 
        bad command
    - name: First1
      id: first
      if: ${{ !always() || true }}
      run: |
        echo dd
    - name: First2
      if: ${{ always() }}
      run: |
        echo hi2
    - name: Use Output 
      run: |
        echo "${{ steps.first.outputs.test_output }}"
        echo "${{ steps.first.outputs.test_output_t }}"
        echo "${{ steps.notexists.outputs.test_output_t }}"
    - name: Operations 1
      if: 
      run: echo "${{ 'false' || '100' }}"
    - name: Operations 2
      if: >- 
        'false'
      run: echo "${{ 'false' || '100' }}"
    - name: Operations 3
      if: "${{ false }}"
      run: echo "${{ 'false' || '300' }}"
    - name: Operations 4
      if: 'true'
      run: echo "${{ 'false' || '500' }}"
    - name: Operations ! 2
      if: false
      run: echo " !'false' = ${{ !'false' }},  !0 = ${{ !0 }}"
    - name: Operations nul == something
      run: echo "null == null = ${{  null == null }}, null == 6 =  ${{ null == 6 }}, null == 'string' = ${{ null == 'string' }}"
    - name: Operations nul != something
      run: echo "null != null = ${{  null != null }}, null != 6 =  ${{ null != 6 }}, null != 'string' = ${{ null != 'string' }}"
    - name: Operations null > something
      run: echo "null > null = ${{  null > null }}, null > 6 =  ${{ null > 6 }}, null > 'string' = ${{ null > 'string' }}"
    - name: Operations null >= something
      run: echo "null >= null = ${{  null >= null }}, null >= 6 =  ${{ null >= 6 }}, null >= 'string' = ${{ null >= 'string' }}"
    - name: Operations equal tricky
      run: echo "5 == '5' = ${{ 5 == '5'}}, '5.0' == '5'=  ${{ '5.0' == '5' }} '5.0' == 5 =  ${{ '5.0' == 5 }}"

    - name: Operations
      run: echo "5 > 6 = ${{ 5 > 6 }}, 5 < 6 =  ${{ 5 < 6 }}, 5 < 'dd' = ${{ 5 < 'dd' }}, 5 > 'dd' = ${{ 5 > 'dd' }}"
    - name: Operations 2
      run: echo "'d' > 'dd' = ${{ 'd' > 'dd' }}, 'e' > 'dd' =  ${{ 'e' > 'dd' }}, true < false = ${{ true < false }}, true > true = ${{ true > true }}"
    - name: Operations 3
      run: echo "true >= true = ${{ true >= true }}, false < true =  ${{ false < true }}, true < false = ${{ true < false }}, true > true = ${{ true > true }}"
    - name: Check string truthiness for ${{ false || '' }}
      run: echo "At least one string is truthy"
      if: ${{ false || 'ddd' }}
    - name: Check string truthiness for false || ''
      run: echo "At least one string is truthy"
      if: ${{ false || '' }}
    - name: Check string truthiness for false || 'false'
      run: echo "At least one string is truthy"
      if: ${{ false || 'false' }}
    - name: Check string truthiness for false || '0'
      run: echo "At least one string is truthy"
      if: ${{ false || '0' }}
    - name: Check string truthiness for false || 0
      run: echo "At least one string is truthy"
      if: ${{ false || 0 }}
    - name: Check string truthiness for false || 1
      run: echo "At least one string is truthy"
      if: ${{ false || 1 }}
    - name: Check string truthiness for false || '00'
      run: echo "At least one string is truthy"
      if: ${{ false || '00' }}
    - name: Check string truthiness for false || '1'
      run: echo "At least one string is truthy"
      if: ${{ false || '1' }}
    - name: Check string truthiness for false || false 
      run: echo "At least one string is truthy"
      if: ${{ false || false }}
    - name: Check string truthiness for false || false without expression syntax
      run: echo "At least one string is truthy"
      if: false || false
